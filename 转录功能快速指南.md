# 转录功能快速指南

## 一分钟快速上手

### 1. 安装依赖
```bash
pip install openai-whisper
```

或者重新安装所有依赖：
```bash
pip install -r requirements.txt
```

### 2. 启用转录功能
编辑 `config/config.ini` 文件，找到 `[转录设置]` 部分：

```ini
[转录设置]
是否开启转录功能(是/否) = 是
```

### 3. 开始使用
运行录制程序，录制完成后会自动生成字幕文件！

## 常见问题

### Q: 如何选择合适的模型？
**A:** 推荐使用 `base` 模型，它在速度和准确度之间取得了良好平衡。
- 如果电脑配置较低或需要快速转录：使用 `tiny`
- 如果追求更高准确度且不在意速度：使用 `small` 或 `medium`
- 如果有高性能GPU且追求最佳效果：使用 `large`

### Q: 转录速度慢怎么办？
**A:** 
1. 使用较小的模型（如 `tiny` 或 `base`）
2. 指定语言而不是自动检测（如设置为 `zh`）
3. 如果有NVIDIA GPU，设置为使用CUDA加速
4. 升级硬件（更快的CPU或添加GPU）

### Q: 转录不准确怎么办？
**A:**
1. 使用更大的模型（如 `medium` 或 `large`）
2. 明确指定语言（如 `zh` 表示中文，`en` 表示英文）
3. 确保视频音质清晰
4. 检查是否有明显的背景噪音

### Q: 首次使用需要下载什么？
**A:** 首次使用时会自动下载选定的Whisper模型文件：
- tiny: ~75MB
- base: ~150MB
- small: ~500MB
- medium: ~1.5GB
- large: ~3GB

模型文件会缓存在本地，之后不需要重复下载。

### Q: 支持哪些语言？
**A:** Whisper支持99种语言，包括但不限于：
- 中文 (zh)
- 英语 (en)
- 日语 (ja)
- 韩语 (ko)
- 法语 (fr)
- 德语 (de)
- 西班牙语 (es)
等等...

完整列表请参考：https://github.com/openai/whisper#available-models-and-languages

### Q: 转录文件保存在哪里？
**A:** 转录文件与录制的视频保存在同一目录，文件名相同但扩展名不同。
例如：
- 视频文件: `主播名_2024-02-01_12-30-00.mp4`
- 字幕文件: `主播名_2024-02-01_12-30-00.srt`

### Q: 可以只转录不录制吗？
**A:** 当前版本转录功能是录制后自动触发的。如果需要单独转录已有视频，可以使用以下方法：

```python
from src.transcriber import transcribe_file

# 转录单个视频文件
transcribe_file(
    video_path="path/to/video.mp4",
    model_name="base",
    language="zh",
    output_format="srt"
)
```

### Q: 转录会影响录制吗？
**A:** 不会。转录在后台线程中运行，不会影响正在进行的录制。

### Q: 需要什么配置的电脑？
**A:** 最低要求：
- CPU: 双核及以上
- 内存: 4GB（使用tiny或base模型）
- 硬盘: 根据模型大小需要几百MB到几GB

推荐配置：
- CPU: 4核及以上
- 内存: 8GB及以上
- GPU: NVIDIA GPU（支持CUDA）可大幅提升速度

## 配置示例

### 基础配置（适合大多数用户）
```ini
[转录设置]
是否开启转录功能(是/否) = 是
转录模型(tiny/base/small/medium/large) = base
转录语言(留空自动检测,zh/en等) = zh
转录输出格式(srt/txt/vtt/json) = srt
转录设备(auto/cpu/cuda) = auto
```

### 快速模式（配置较低的电脑）
```ini
[转录设置]
是否开启转录功能(是/否) = 是
转录模型(tiny/base/small/medium/large) = tiny
转录语言(留空自动检测,zh/en等) = zh
转录输出格式(srt/txt/vtt/json) = srt
转录设备(auto/cpu/cuda) = cpu
```

### 高质量模式（有GPU的高配电脑）
```ini
[转录设置]
是否开启转录功能(是/否) = 是
转录模型(tiny/base/small/medium/large) = large
转录语言(留空自动检测,zh/en等) = zh
转录输出格式(srt/txt/vtt/json) = srt
转录设备(auto/cpu/cuda) = cuda
```

### 多语言自动检测模式
```ini
[转录设置]
是否开启转录功能(是/否) = 是
转录模型(tiny/base/small/medium/large) = base
转录语言(留空自动检测,zh/en等) = 
转录输出格式(srt/txt/vtt/json) = srt
转录设备(auto/cpu/cuda) = auto
```

## 故障排除

### Whisper安装失败
如果安装openai-whisper时遇到问题，尝试：
```bash
pip install --upgrade pip setuptools wheel
pip install openai-whisper
```

### GPU不被识别
确保已安装NVIDIA驱动和CUDA工具包。检查：
```bash
nvidia-smi  # 查看GPU状态
```

如果没有GPU或CUDA，将设备设置为 `cpu` 即可。

### 转录卡住不动
1. 检查视频文件是否完整且未损坏
2. 查看日志文件了解详细错误信息
3. 尝试使用更小的模型
4. 确保有足够的磁盘空间和内存

## 获取帮助

如果遇到问题：
1. 查看项目 Issues: https://github.com/tonyy01/DouyinLiveRecorder-Transcriber/issues
2. 阅读 Whisper 官方文档: https://github.com/openai/whisper
3. 提交新的 Issue 描述您的问题
